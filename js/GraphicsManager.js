var GraphicsManager = function () {
    var canvas = null;

    var wall_color = null;
    var ball_color = null;

    // intialize HTML5 canvas.
    // @param canvas_container Div container for the HTML5 canvas
    // @param width The width of the canvas
    // @param height The height of the canvas
    self.init = function (canvas_container, width, height) {

    };

    // draw objects to the screen
    // @param objects Array of objects to draw
    self.draw = function (objects) {

    };
};